name: Test working-directory input
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Create working directory with .ruby-version file
        run: |
          mkdir -p /tmp/myapp
          echo "3.1" > /tmp/myapp/.ruby-version
      - id: action
        uses: .
        with:
          working-directory: '/tmp/myapp'
      - name: Assert ruby-prefix output is set
        env:
          ACTUAL: ${{ steps.action.outputs.ruby-prefix }}
        run: |
          echo "ruby-prefix: $ACTUAL"
          if [ -z "$ACTUAL" ]; then
            echo "FAIL: expected ruby-prefix to be non-empty"
            exit 1
          fi
          echo "PASS"
      - name: Assert ruby is available
        run: |
          ruby --version
          echo "PASS"
